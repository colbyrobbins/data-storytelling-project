# Notebooks

Exploratory / scratch notebooks live here — a good place to poke at the data, try out a chart, or sanity-check an idea before it becomes real code.

Anything that needs to run for someone else to reproduce the analysis (cleaning, feature engineering, the final EDA charts) should end up as a script in `src/`, not only live in a notebook.
