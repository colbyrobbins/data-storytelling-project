# Processed Data

Cleaned data and engineered features produced by the scripts in `src/` go here (outputs of `preprocessing.py` and `features.py`).

Nothing in this folder should be edited by hand — regenerate it by re-running the pipeline described in the main README.
